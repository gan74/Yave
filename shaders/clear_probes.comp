#version 450

#include "lib/surfels.glsl"

// -------------------------------- I/O --------------------------------

layout(local_size_x = 8, local_size_y = 8) in;

layout(r32ui, set = 0, binding = 0) uniform uimage3D out_probes;


// -------------------------------- MAIN --------------------------------

void main() {
    imageStore(out_probes, ivec3(gl_GlobalInvocationID), uvec4(0));
}

